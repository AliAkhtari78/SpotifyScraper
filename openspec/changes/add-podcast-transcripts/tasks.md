# Tasks: add-podcast-transcripts

## 0. Live verification (BLOCKS api/transcripts.py decode_envelope — do with maintainer)

- [ ] 0.1 With the maintainer `SPOTIFY_SP_DC`, capture a real response from
      `GET https://spclient.wg.spotify.com/transcript-read-along/v2/episode/{id}?format=json`
      with headers `Authorization: Bearer <token>`, `app-platform: WebPlayer`.
      Use a known episode that has "Read along" in the web player.
- [ ] 0.2 Record: (a) exact non-200 codes (expect 404 = no transcript, 401/403 =
      auth); (b) whether the 200 body is plain JSON or a JSON envelope wrapping a
      base64 protobuf blob; (c) the key the transcript lives under; (d) whether
      `language` / `isAutoGenerated` / `provider` appear at the JSON top level.
- [ ] 0.3 If base64+protobuf: confirm the field numbers against the community
      schema (`segments`=4 → `time_ms`=2, `transcription`=3 → `segment`=1 repeated
      string) on a current episode.
- [ ] 0.4 Confirm whether `market=from_token` and/or `accept-language` are
      required/accepted; note the answer in design.md.
- [ ] 0.5 Scrub the captured body (no `accessToken`/`clientId`/cookie) and pin the
      normalized envelope shape in design.md as the contract `parse_transcript` targets.

## 1. Model

- [ ] 1.1 Create `src/spotify_scraper/models/transcript.py`: frozen slotted
      `TranscriptLine(start_ms, text)` and `Transcript(lines, language=None,
      provider=None, is_auto_generated=None)` on `ModelBase`.
- [ ] 1.2 Export `Transcript, TranscriptLine` from `models/__init__.py` and from
      the top-level `__init__.py` (import block + `__all__`, alphabetical).

## 2. Sans-io fetch + parse

- [ ] 2.1 Create `src/spotify_scraper/api/transcripts.py`: endpoint isolated in
      module constants; `transcript_url(episode_id)`, `auth_headers(token)`, and
      `decode_envelope(raw)` normalizing the verified shape into
      `{"lines": [{"start_ms", "text"}, ...], "language", "provider", "isAutoGenerated"}`.
      Pure / I/O-free. A tiny hand-rolled protobuf-wire reader lives here if needed
      (no new dependency).
- [ ] 2.2 Add `parse_transcript(payload)` to `api/parse_entities.py` (new section,
      reuse existing helpers, drop empty-text lines, `ParsingError` + update hint
      when the lines container is missing, add to `__all__`).

## 3. Client exposure (shared provider)

- [ ] 3.1 Rename `_lyrics_tokens`→`_cookie_tokens`, `_lyrics_provider`→
      `_cookie_provider` on both clients; generalize the "requires authentication"
      message; `get_lyrics` calls the renamed builder. Behavior identical.
- [ ] 3.2 Add `get_transcript(value)` + `_fetch_transcript(entity_id, token)` to
      both clients (resolve `"episode"`, shared provider, 404→NotFoundError,
      401→invalidate+retry-once, undecodable→ParsingError). Async mirror.

## 4. CLI

- [ ] 4.1 Add a `transcript` command to `cli/main.py` mirroring `lyrics`;
      generalize the `_resolve_cookies` `BadParameter` wording away from "Lyrics".

## 5. Tests

- [ ] 5.1 `tests/unit/api/test_parse_transcript.py`: pure parse over a payload
      builder; line count/text/start_ms, dropped-empty, missing-container ParsingError.
- [ ] 5.2 `tests/unit/api/test_transcripts_api.py`: `transcript_url`, `auth_headers`,
      and `decode_envelope` over a known normalized + (if used) protobuf vector.
- [ ] 5.3 `tests/unit/test_client_transcript.py` (respx, sync + async): no-cookies
      → AuthenticationError with 0 network calls; success header asserts; 404 →
      NotFoundError; 401 → retry-once; anonymous cookie; use-after-close.
- [ ] 5.4 `tests/unit/cli/test_cli_transcript.py`: FakeClient, JSON emit, cookie
      resolution, `--pretty`, exit codes 3/4.
- [ ] 5.5 `tests/live/test_transcript.py`: `@pytest.mark.live`, skip without
      `SPOTIFY_SP_DC`; sync + async smoke.

## 6. Verify

- [ ] 6.1 `make lint`, `make type`, `make test`, `make cov` (≥85%) all green.
- [ ] 6.2 `make live` (with `SPOTIFY_SP_DC`) green; confirm captured envelope still
      matches `decode_envelope`.
- [ ] 6.3 Docs: add transcripts to the authenticated-features page next to lyrics.
